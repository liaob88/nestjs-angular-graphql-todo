type Task {
  id: ID!
  title: String!
  task: String!
}

input InputTask {
  title: String!
  task: String!
}

type Mutation {
  createTask(task: InputTask!): String!
}

type Query {
  getTasks: [Task!]!
  getTask(id: ID!): Task!
}
